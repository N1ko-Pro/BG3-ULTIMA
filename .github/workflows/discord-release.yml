name: Discord Release Notification

on:
  release:
    types: [published]

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Send Discord notification
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK_URL }}
          RELEASE_TAG: ${{ github.event.release.tag_name }}
          RELEASE_NAME: ${{ github.event.release.name }}
          RELEASE_BODY: ${{ github.event.release.body }}
          RELEASE_URL: ${{ github.event.release.html_url }}
          RELEASE_AUTHOR: ${{ github.event.release.author.login }}
          RELEASE_DATE: ${{ github.event.release.published_at }}
        run: |
          PAYLOAD=$(jq -n \
            --arg title "🚀 Новый релиз BG3 ULTIMA — $RELEASE_TAG" \
            --arg thread "🚀 BG3 ULTIMA $RELEASE_TAG" \
            --arg url "$RELEASE_URL" \
            --arg desc "$RELEASE_BODY" \
            --arg tag "$RELEASE_TAG" \
            --arg ts "$RELEASE_DATE" \
            '{
              thread_name: $thread,
              embeds: [{
                title: $title,
                url: $url,
                description: $desc,
                color: 5793266,
                fields: [
                  { name: "📦 Версия", value: $tag, inline: true },
                  { name: "👤 Автор",  value: "ANICKON", inline: true }
                ],
                footer: { text: "BG3 ULTIMA • GitHub Releases" },
                timestamp: $ts
              }]
            }')
          curl -s -X POST "$DISCORD_WEBHOOK" \
            -H "Content-Type: application/json" \
            -d "$PAYLOAD"
